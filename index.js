export const sum = (a, b) => {
  return a + b
}

// Beispielhafte Nutzung der Funktion:
const result = sum(1, 2)
console.log(result)

// Vergleich mit ===:
if (result === 4) {
  console.log('It equals 4')
}
